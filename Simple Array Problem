#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,i,arr[1000];
    scanf("%d",&n);
    int sum=0;
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
        sum=sum+arr[i];
    }
    printf("%d",sum);
return 0;
}
